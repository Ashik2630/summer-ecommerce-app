import ProfileClient from "@/components/shared/ProfileClient";


export const metadata = {
  title: "SummerCart || ProfilePage",
};

const ProfilePage = () => {
  return <ProfileClient />;
};

export default ProfilePage;